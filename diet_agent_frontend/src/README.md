# Frontend Src Module

## 作用

Vue 3 前端主代码目录，负责路由渲染、页面交互与 API 调用。

## 目录说明

- `App.vue`: 应用壳与侧边导航。
- `main.js`: 应用启动入口。
- `api.js`: Axios 实例配置。
- `views/`: 页面模块。
- `router/`: 路由配置与鉴权跳转。
- `utils/`: 公共工具函数。

## 开发约定

- 页面状态优先就地管理，避免过早引入全局状态。
- 所有后端请求经 `api.js` 统一管理。
