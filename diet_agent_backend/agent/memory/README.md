# Memory Module

## 作用

管理用户长期偏好与短期行为记忆，支持推荐与反馈学习。

## 文件说明

- `semantic.py`: 语义记忆（偏好、禁忌、正负反馈）。
- `episodic.py`: 情景记忆（近期饮食事件）。
- `working.py`: 工作记忆（会话压缩与短期状态）。
- `manager.py`: 记忆编排入口。

## 使用方式

业务层通过 `MemoryManager` 统一读写，避免在业务代码中直接分散调用各记忆子模块。
