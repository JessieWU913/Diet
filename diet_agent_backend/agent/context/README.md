# Context Module

## 作用

构建对话上下文，组合用户画像、近期饮食与策略模板，供智能问答链路使用。

## 文件说明

- `builder.py`: 上下文聚合主逻辑。
- `budget.py`: 预算与配额计算辅助。
- `template.py`: 上下文模板与格式化。

## 输入输出

- 输入：`user_id`、模式（标准/减脂）、请求文本。
- 输出：结构化上下文片段（画像、近期记录、约束条件）。

## 扩展建议

- 新增上下文字段时，保持字段名稳定，避免前后端联动破坏。
